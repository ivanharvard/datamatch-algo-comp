#include <set>
#include <cassert>
#include <numeric>
#include <algorithm>
#include <cstdio>

void run(int target, int limit,
         int** sorted_indices,
         int** scores,
         bool** matched,
         int* cur_poss,
         int ucount,
         std::set<int>* match_sets);

/// run works on a large input

int main(void) {
    
    return 0;
}